import { StyleSheet } from 'react-native';
import themeColors from '../../constants/themeColors';

const styles = StyleSheet.create({
  header: {
    backgroundColor: themeColors.headerAuthenticated.backgroundColor,
  },
});

export default styles;
